import Vue from 'vue'
import {
  Form,
  Button,
  Input,
  FormItem
  // Message
  // MessageBox
} from 'element-ui'

Vue.use(Form)
Vue.use(Button)
Vue.use(Input)
Vue.use(FormItem)
// Vue.use(Message)
// Vue.use(MessageBox)


// Vue.prototype.$message = Message
// Vue.prototype.$confirm = MessageBox.confirm
